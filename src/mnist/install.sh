# Upgrade pip
python3 -m pip install --upgrade pip

# Install PyTorch and torchvision
python3 -m pip install torch torchvision

# Install TensorFlow
python3 -m pip install tensorflow

# Install the MNIST dataset handler
python3 -m pip install mnist
